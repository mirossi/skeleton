<div class="panel panel-default">
  <div class="panel-heading">
    <h1 class="panel-title"><strong>Wein </strong></h1></div>
  <div class="panel-body">
    <%= render 'form' ,:static_show => true, :extrabuttons => "#{link_to 'Editieren',  edit_wine_path(@wine)  , :class => 'btn btn-default', :id => "MyBackButton"} #{link_to 'Zurück', wines_path , :class => 'btn btn-primary', :id => "MyBackButton"}" %>
  </div>
</div>

